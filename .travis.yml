language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-esri-bootstrap-map' ]; then cd .. && eval "mv $currentfolder generator-esri-bootstrap-map" && cd generator-esri-bootstrap-map; fi

